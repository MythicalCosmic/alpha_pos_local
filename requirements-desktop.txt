# Local (desktop) edition dependencies. The shared spine (Django, channels,
# psycopg v3, redis, cryptography, anthropic, ...) comes from the alpha_pos_core
# git submodule installed here:
./alpha_pos_core
# ASGI server: the POS serves HTTP + websockets via uvicorn (replaces waitress).
uvicorn[standard]==0.49.0
daphne==4.2.1
# Native desktop window (WebView2) + build-time icon generation.
pywebview
pythonnet
Pillow
# Signed self-update engine.
tufup
